/// This is an enum to represent the fixed set of difficulty levels
/// for the questions.

public enum Difficulty {
    EASY,     // Represents the Easy difficulty
    MEDIUM,   // Represents the Medium difficulty
    DIFFICULT // Represents the Difficult difficulty
}